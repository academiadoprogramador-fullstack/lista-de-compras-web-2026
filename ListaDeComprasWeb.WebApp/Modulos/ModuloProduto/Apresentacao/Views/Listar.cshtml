@{
    Layout = "_Layout";
    ViewBag.Titulo = "Listagem de Produtos";
}

@model List<ListarProdutosViewModel>;

<a class="btn btn-outline-primary rounded-pill px-4 mb-3" asp-action="Cadastrar">Cadastrar Novo</a>

@if (TempData["MensagemErro"] is string mensagemErro)
{
    <div class="alert alert-danger border-start border-danger border-4 rounded-3" role="alert">
        @mensagemErro
    </div>
}

@if (Model.Count == 0)
{
    <div class="alert alert-primary border-start border-primary border-4 rounded-3" role="alert">
        Nenhum produto cadastrado.
    </div>
}
else
{
    <div class="row row-cols-1 row-cols-md-2 row-cols-xl-3 g-2">
        @foreach (ListarProdutosViewModel p in Model)
        {
            <div class="col">
                <div class="card h-100">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-start gap-3 mb-3">
                            <h5 class="card-title mb-0">@p.Nome</h5>
                            <span class="badge text-bg-primary rounded-pill">@p.Id.ToString().Substring(0, 8)</span>
                        </div>

                        <dl class="row g-2 mb-4">
                            <dt class="col-6 text-secondary fw-semibold">Categoria</dt>
                            <dd class="col-6 mb-0 text-break">
                                <span class="d-inline-block rounded-circle border me-2 align-middle" style="width: 1rem; height: 1rem; background-color: @p.CategoriaCor;"></span>
                                @p.CategoriaNome
                            </dd>

                            <dt class="col-6 text-secondary fw-semibold">Unidade</dt>
                            <dd class="col-6 mb-0 text-break">@p.UnidadeMedida</dd>

                            <dt class="col-6 text-secondary fw-semibold">Preço Aproximado</dt>
                            <dd class="col-6 mb-0 text-break">@p.PrecoAproximado.ToString("C")</dd>
                        </dl>

                        <div class="d-flex flex-wrap gap-2 mt-auto justify-content-end">
                            <a class="btn btn-outline-primary btn-sm rounded-pill px-3" asp-action="Editar" asp-route-id="@p.Id">Editar</a>
                            <a class="btn btn-outline-primary btn-sm rounded-pill px-3" asp-action="Excluir" asp-route-id="@p.Id">Excluir</a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
}
